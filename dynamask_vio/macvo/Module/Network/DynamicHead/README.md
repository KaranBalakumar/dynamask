StaticConfidenceHead implements a recurrent per-pixel confidence predictor over frozen FlowFormer context/flow/covariance and AirIMU conditioning, with optional proxy channels, dual static-visible outputs, H/4 refinement, and monotonic solver-weight mapping.

